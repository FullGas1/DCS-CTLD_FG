*.iml
.idea/encodings.xml
*.xml

# CTLD_Next.lua is intentionally tracked — available at repo root for DCS missions.
# Do NOT add it to .gitignore.

# ── Local-only recette resources (never commit) ───────────────────────────────
# All local debug/diagnostic/patch scripts live in recette/local/.
# New files created there are automatically excluded from git.
recette/local/